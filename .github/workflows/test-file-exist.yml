name: "Check File Existence"

on: [push, pull_request]

jobs:
  check_file_existence:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check file existence
        if: ${{ hashFiles('skdfjoisdifujsdf.html') != '' }}
        run: echo "All files exist!"
